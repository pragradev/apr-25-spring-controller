spring:
  application:
    name: java-apr-2025-spring-controller
  datasource:
    url: jdbc:h2:./appdb
    username: sa
    password:


  h2:
    console:
      enabled: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8082
